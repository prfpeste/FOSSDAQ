# removing FOSSDAQ form the Raspberry PI

To completely remove FOSSDAQ from the Raspberry PI, run `sudo bash ~/firmware/uninstall.sh` and `sudo rm -rf ~/firmware` in the shell. 
This will revert any changes made to your system by FOSSDAQ.
